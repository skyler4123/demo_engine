<%= form_with(model: sub_season) do |form| %>
  <% if sub_season.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sub_season.errors.count, "error") %> prohibited this sub_season from being saved:</h2>

      <ul>
        <% sub_season.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :season_id, style: "display: block" %>
    <%= form.number_field :season_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
